# @package _global_

defaults:
  - override /data: libero
  - override /model: easywam_unified_cosmos25_lora
  - _self_

batch_size: 8
num_workers: 8

lr_scheduler_type: cosine
learning_rate: 1e-4
max_steps: 40000
log_every: 100
save_every: 4000
eval_every: 1000
eval_num_inference_steps: 10

gradient_accumulation_steps: 1
weight_decay: 1e-2
resume: null
